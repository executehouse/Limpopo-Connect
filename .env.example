# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these values from https://app.supabase.com/project/_/settings/api

# Public Supabase URL (safe to expose in frontend)
# This is your Supabase project URL - visible in frontend code
VITE_SUPABASE_URL=https://sscsjwaogomktxqhvgxw.supabase.co

# Public anonymous key (safe to expose in frontend)
# This key has limited permissions controlled by Row Level Security (RLS)
# Supabase Dashboard → Settings → API → anon public key
VITE_SUPABASE_ANON_KEY=pk.public-anon-key-placeholder

# ⚠️ SENSITIVE: Service role key (NEVER expose in frontend!)
# This key bypasses ALL RLS policies and has full database access
# ONLY use in server-side code: Vercel API routes, Supabase Edge Functions
# Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=sk.service-role-key-placeholder

# ============================================
# VITE CONFIGURATION
# ============================================
# Base path for the application (usually '/' for root deployment)
# Set to subdirectory path if deploying to GitHub Pages or subdomain
VITE_BASE=/

# ============================================
# VERCEL DEPLOYMENT NOTES
# ============================================
# For Vercel deployment, set these environment variables in:
# Vercel Dashboard → Project Settings → Environment Variables
# 
# Required for ALL environments (Preview + Production):
#   - VITE_SUPABASE_URL
#   - VITE_SUPABASE_ANON_KEY
#   - VITE_BASE (optional, defaults to '/')
#
# Required for SERVER-SIDE API routes only:
#   - SUPABASE_SERVICE_ROLE_KEY (mark as SECRET in Vercel)
#
# Build Settings:
#   - Build Command: npm run build
#   - Output Directory: dist
#   - Install Command: npm install
#   - Node Version: 18.x or later

# ============================================
# LOCAL DEVELOPMENT SETUP
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Get your Supabase credentials from: https://app.supabase.com/project/_/settings/api
# 3. Replace placeholder values above with real credentials
# 4. Start dev server: npm run dev
# 5. NEVER commit .env.local or real credentials!

# ============================================
# EXTERNAL API KEYS (OPTIONAL INTEGRATIONS)
# ============================================
# These are optional third-party APIs for enhanced functionality
# All are safe to expose in frontend (have rate limits and free tiers)

# IPInfo.io - Geolocation detection (Free tier: 50k requests/month)
# Sign up at: https://ipinfo.io/signup
VITE_IPINFO_TOKEN=your-ipinfo-token

# OpenWeatherMap - Weather data (Free tier: 1k requests/day)
# Sign up at: https://openweathermap.org/api
VITE_OPENWEATHERMAP_API_KEY=your-openweathermap-key

# NewsData.io - News feed (Free tier: 200 requests/day)
# Sign up at: https://newsdata.io/register
VITE_NEWSDATA_API_KEY=your-newsdata-key

# Unsplash - Hero images (Free tier: 50 requests/hour)
# Sign up at: https://unsplash.com/developers
VITE_UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# Mapbox - Map display (Free tier: 50k loads/month)
# Sign up at: https://account.mapbox.com/
VITE_MAPBOX_TOKEN=your-mapbox-token

# ExchangeRate API - Currency conversion (Free tier: 1500 requests/month)
# No signup required: https://exchangerate.host
# Note: This API is free and doesn't require a key

# Formspree - Contact form (Free tier: 50 forms/month)
# Sign up at: https://formspree.io/
VITE_FORMSPREE_FORM_ID=your-formspree-form-id

# ============================================
# SECURITY CHECKLIST
# ============================================
# ✓ .env.local is in .gitignore
# ✓ Service role key ONLY in server-side code
# ✓ Anon key used in frontend (RLS protects data)
# ✓ All database tables have RLS enabled
# ✓ Environment variables set in Vercel dashboard
# ✓ Service role key marked as SECRET in Vercel
